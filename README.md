# RetirePlan

RetirePlan is a single-window retirement planning GUI.

The controlling project philosophy, scope, and development contract is
[docs/PROJECT_CONTRACT.md](docs/PROJECT_CONTRACT.md). Use that document as the
authority for project purpose, feature admission, variable discipline, and
development workflow.

Run:

```powershell
python -m retireplan.gui
```
